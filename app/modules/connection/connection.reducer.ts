const initialState = {
  pending: true,
  error: false,
}

const connectionReducer = (state = initialState, action: any): typeof initialState => {
  return state;
}

export default connectionReducer;
